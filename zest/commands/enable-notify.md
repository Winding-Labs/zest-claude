---
description: Enable Zest status line notifications
allowed-tools: Bash
---

Execute the Zest enable notifications script:

```bash
${CLAUDE_PLUGIN_ROOT}/bin/zest command enable-notify
```

Show the complete output directly to the user without modification.
