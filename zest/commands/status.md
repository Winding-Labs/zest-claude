---
description: View Zest tracker authentication and sync status
allowed-tools: Bash
---

Execute the Zest tracker status script and display the output to the user:

```bash
node ${CLAUDE_PLUGIN_ROOT}/dist/commands/status-cli.js
```

Show the complete output directly to the user without modification.
