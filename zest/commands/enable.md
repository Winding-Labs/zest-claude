---
description: Enable remote persistence (sync data to Zest)
allowed-tools: Bash
---

Execute the Zest enable script to turn on remote syncing:

```bash
node ${CLAUDE_PLUGIN_ROOT}/dist/commands/enable-cli.js
```

Show the complete output directly to the user without modification.

